//! Workflow proposal desktop UI components (placeholder).
//!
//! Desktop-gated render components for workflow proposals.
//! Enabled only with the `desktop` feature.

#[cfg(feature = "desktop")]
pub fn workflow_proposal_panel() {
    // Desktop UI rendering will be added in a future wave.
}
