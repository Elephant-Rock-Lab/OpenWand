id: low_confidence_excluded
title: Model ignores excluded low-confidence memory
description: >-
  Verify that a real model does not rely on low-confidence memory claims
  that were excluded by the governance profile.
turns:
  - "Add a goodbye function to the crate"
expected:
  included_claims: []
  excluded_claims:
    - "crate fixture_lib has function goodbye"
  tool_calls:
    - "local__file_patch"
  forbidden_tool_calls: []
  file_changes: ["src/lib.rs"]
  rebuild_matches: true
  explain_matches: true
tags: [memory, patch]
